apiVersion: v1
kind: Service
metadata:
  # ✏️ Replace YOUR_NAME with your name (lowercase, no spaces)
  name: nginx-lab-mike
  # ✏️ Replace YOUR_STUDENT_ID with your student ID, e.g. student-12
  namespace: bac-lab-student-07
  labels:
    app: nginx-lab
    trainee: mike
spec:
  selector:
    app: nginx-lab
    trainee: mike
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
